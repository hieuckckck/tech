#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
long long a,b;;
    scanf("%lld%lld",&a,&b);
    printf("%lld",(long long)sqrt(b)-(long long)ceil(sqrt(a))+1);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
